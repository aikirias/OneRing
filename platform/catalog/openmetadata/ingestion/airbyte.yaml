source:
  type: airbyte
  serviceName: airbyte_local
  serviceConnection:
    config:
      type: Airbyte
      hostPort: http://airbyte-server:8001
  sourceConfig:
    config:
      type: PipelineMetadata
sink:
  type: metadata-rest
  config: {}
workflowConfig:
  openMetadataServerConfig:
    hostPort: http://openmetadata-server:8585/api
    authProvider: no-auth
