name: oner_dbt
version: 1.0.0
config-version: 2

profile: oner_dbt

model-paths: ["models"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]

models:
  oner_dbt:
    staging:
      +schema: staging
      +tags: ["staging"]
    marts:
      +schema: marts
      +materialized: view
